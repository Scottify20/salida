import { ElementScrollPositionSaverDirective } from './element-scroll-position-saver.directive';

describe('ElementScrollPositionSaverDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementScrollPositionSaverDirective();
    expect(directive).toBeTruthy();
  });
});
