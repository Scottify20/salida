import { CapsLockDetectorDirective } from './caps-lock-detector.directive';

describe('CapsLockDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new CapsLockDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
